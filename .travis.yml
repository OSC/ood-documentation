sudo: required
services:
  - docker
before_install:
  - docker pull nickjer/docker-sphinx
script:
  - docker run --rm -i -t -v "${PWD}:/doc" nickjer/docker-sphinx make html
after_success:
  - ./push.sh
